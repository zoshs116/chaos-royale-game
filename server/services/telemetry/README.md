# Telemetry Service

## Planned ownership

1. Match runtime metrics ingestion (`tick`, `rtt`, `jitter`, `drop`, `desync`).
2. Alert materialization for anomaly detection.
3. Export feeds for balancing and anti-cheat analytics.

## Trigger

- Consumes match lifecycle events and simulator summaries.
