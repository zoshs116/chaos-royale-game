Map assets are organized by arena.

Each arena should provide a `manifest.json` and optional visual layers. The battle logic remains in logical coordinates; visual layers can be replaced without changing unit placement rules.

